package problem27;

public class Problem27 {
    public boolean isPalindrome(ListNode head) {
        // Come up with a recursive approach
        if (head != null) {
            int headValue = head.val;
            //return headValue == lastNode(head.next)
        }
        return true;
    }
}
